Tests
-----

Run the lightweight tests with Node (requires Node >= 14). No dependencies.

```bash
npm test
```

The runner executes a few assertions for the `german-core` helper module using the `src/data/words.json` fixture.
